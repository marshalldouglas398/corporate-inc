My opinions based on implementing things

User.java:
 - The User class does not have a Question anymore with QuestionList; 
   it's the Student, Editor, and Admin that technically have the question. 
   This might mean that the arrow should be pointing from those three to 
   Question.
 - The resetPassword() function was private and never used inside the class.
   I think it should be a public function, so I made it public. It also had no
   password to change the password to, so I added that as a parameter. If I'm 
   missing something, just change it back.
 - To create a copy constructor by UUID, I need to be able to get a user's
   attributes by UUID, which means I need to be able to query for a user by 
   UUID. I'm going to add that. The UML only has getUser(String username, 
   String password)

Comment.java:
 - The UML has no way to do an average of the ratings. I have added that. Just 
   remove if I've made a mistake.
 - The comment has no idea what question it is on, so it definitely has no
   idea who the question author is, so I can't set it in the constructor or in
   any other thing. We could work with it as a setter and then implement it with 
   a different add comment closer to the UI but not embedded in the comment itself.

Question.java:
 - I did the same rating average trick for the question's rating.
 - I have no idea what to do about the timer in setInterviewMode(). That may come in later
   functionality.

Admin.java:
 - I think that editUser() and deleteUser() should be public and I think that they
   should take a UUID instead of a username.

The tasks that are in the in Progress section are being held up by the following:
 - Implement Admin class: We don't have any database operations yet to work with users
 - Implement addRating method + Implement rateComment method: I want consensus on adding a 
   numRatings element for calculating average rating before I consider the issue closed
 - Implement setInterviewMode method: I have no idea how the timing will work with this, but I 
   still started building it out, so it's "in progress"
 - Implement Editor class: Requires database operations
 - Implement User class: Requires database operations
 - Implement Comment class: I'm not sure how we will figure out if the comment author is the 
   comment's question's author because the comment can't see the question. This alse depends on
   rateComment() consensus to be completed.
 - Implement Question class: Requires database operations for Question construction by UUID, 
   addRating() consensus as before, and implementation of setInterviewMode